{
  "extends": [
    "next",
    "next/core-web-vitals",
    "plugin:import/recommended",
    "plugin:import/errors",
    "plugin:import/warnings"
  ],
  "env": {
    "browser": true,
    "es2021": true,
    "node": true
  },
  "settings": {
    "import/resolver": {
      "alias": {
        "map": [["@", "./src"]],
        "extensions": [".js", ".jsx", ".ts", ".tsx"]
      },
      "node": {
        "extensions": [".js", ".jsx", ".ts", ".tsx"]
      }
    }
  },
  "rules": {
    "import/no-unresolved": "error", // Ensure import errors are caught
    "import/named": "error", // Ensure named imports exist in the imported module
    "import/default": "error", // Ensure a default export exists in the imported module
    "import/namespace": "error", // Ensure imported namespaces contain dereferenced properties
    "import/no-extraneous-dependencies": "error", // Ensure dependencies are correctly listed in package.json
    "import/no-duplicates": "error" // Report repeated import of the same module in multiple places
  }
}
